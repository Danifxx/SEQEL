# Use SQL Server (SQL Express) in production:
DATABASE_URL=mssql+pyodbc://SA:SEQEL123!@localhost/SEQEL?driver=ODBC+Driver+17+for+SQL+Server

# Optional: quick local fallback (uncomment to use)
# DATABASE_URL=sqlite:///./seqel.db

# Host/port for the app
HOST=0.0.0.0
PORT=8000

# Logger/Admin PINs
PIN_LOGGER=0000
PIN_ADMIN=9999

# Sponsor images folder for the big screen (you can change in Admin later)
SPONSOR_PATH=D:\\SEQEL\\Sponsors

# Drone TimeLap bonus applies on top of Win/Lose in groups (Yes/No)
DRONE_TIMELAP_BONUS=Yes
